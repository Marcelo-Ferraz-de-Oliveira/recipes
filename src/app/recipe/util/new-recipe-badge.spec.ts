import { NewRecipeBadge } from './new-recipe-badge';

describe('NewRecipeBadge', () => {
  it('should create an instance', () => {
    const directive = new NewRecipeBadge();
    expect(directive).toBeTruthy();
  });
});
